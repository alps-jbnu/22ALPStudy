#include <bits/stdc++.h>

using namespace std;

// not AC

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

    vector<int>v;
    deque<int> dq;
    int N,L;
    cin>>N>>L;

    v.push_back(0);
    for(int i = 1;i<=N;++i)
    {
        int num;
        cin>>num;
        v.push_back(num);
    }

    for(int i = 1;i<=N;++i)
    {  
        
    }


}