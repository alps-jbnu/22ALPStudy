private void OperatorButton_Click(object sender, RoutedEventArgs e)
        {
            double newNumber;
            double result = 0;

            Button currentButton = (Button)sender;

            if (double.TryParse(resultLabel.Content.ToString(), out lastNumber))
            {
                resLabel.Content = $"{lastNumber}{" "}{currentButton.Content}";

            }
        
            if (sender == additionButton)
                selectedOperator = SelectedOperator.Addition;
            if (sender == subButton)
                selectedOperator = SelectedOperator.Subtraction;
            if (sender == multiplicationButton)
                selectedOperator = SelectedOperator.Multiplication;
            if (sender == slashButton)
                selectedOperator = SelectedOperator.Division;
    }


 private void OperatorButton_Click(object sender, RoutedEventArgs e)
        {
            
            double newNumber = 0;
            double result = 0;
            Button currentButton = (Button)sender;

            if (firstOper == false)
            {
                lastNumber = double.Parse(resultLabel.Content.ToString());
                resLabel.Content = $"{lastNumber}{" "}{currentButton.Content}";
                result = lastNumber;
                firstOper = true;
                secnodOper = false;
            }

            else
               if (sender == additionButton || secnodOper ==false)
            {
                selectedOperator = SelectedOperator.Addition;
                newNumber = double.Parse(resultLabel.Content.ToString());
                result = SimpleMath.Add(lastNumber,newNumber);

                resLabel.Content = $"{result}{" "}{currentButton.Content}";
                resultLabel.Content = result;
                resultHistory.Items.Insert(0, $"{lastNumber}{" "}{additionButton.Content}{" "}{newNumber}{" = "}{result}");
            }
            else if (sender == subButton)
            {
                newNumber = double.Parse(resultLabel.Content.ToString());
                selectedOperator = SelectedOperator.Subtraction;
                result = SimpleMath.Sub(lastNumber, newNumber);
                resLabel.Content = $"{result}{" "}{currentButton.Content}";
                resultHistory.Items.Insert(0, $"{lastNumber}{" "}{additionButton.Content}{" "}{newNumber}{" = "}{result}");
                resultLabel.Content = result;
                secnodOper = true;
            }
            else if (sender == multiplicationButton)
            {
                newNumber = double.Parse(resultLabel.Content.ToString());
                selectedOperator = SelectedOperator.Multiplication;
                result = SimpleMath.Mul(lastNumber, newNumber);
                resLabel.Content = $"{result}{" "}{currentButton.Content}";
                resultHistory.Items.Insert(0, $"{lastNumber}{" "}{additionButton.Content}{" "}{newNumber}{" = "}{result}");
                resultLabel.Content = result;
            }
            else
            {
                newNumber = double.Parse(resultLabel.Content.ToString());
                selectedOperator = SelectedOperator.Division;
                result = SimpleMath.Div(lastNumber, newNumber);
                resLabel.Content = $"{result}{" "}{currentButton.Content}";
                resultHistory.Items.Insert(0, $"{lastNumber}{" "}{additionButton.Content}{" "}{newNumber}{" = "}{result}");
                resultLabel.Content = result;
            }
            number = true;

            if (secnodOper == true)
            {
                resultHistory.Items.Insert(0, $"{newNumber}{" "}{additionButton.Content}{" "}{result}{" = "}{resultLabel.Content}");
            }
        

--------------
else if (sender == subButton)
            {
                newNumber = double.Parse(resultLabel.Content.ToString());
                selectedOperator = SelectedOperator.Subtraction;
                result = SimpleMath.Sub(lastNumber, newNumber);
                resLabel.Content = $"{result}{" "}{currentButton.Content}";
                resultHistory.Items.Insert(0, $"{lastNumber}{" "}{additionButton.Content}{" "}{newNumber}{" = "}{result}");
                resultLabel.Content = result;

            }
            else if (sender == multiplicationButton)
            {
                newNumber = double.Parse(resultLabel.Content.ToString());
                selectedOperator = SelectedOperator.Multiplication;
                result = SimpleMath.Mul(lastNumber, newNumber);
                resLabel.Content = $"{result}{" "}{currentButton.Content}";
                resultHistory.Items.Insert(0, $"{lastNumber}{" "}{additionButton.Content}{" "}{newNumber}{" = "}{result}");
                resultLabel.Content = result;
            }
            else
            {
                newNumber = double.Parse(resultLabel.Content.ToString());
                selectedOperator = SelectedOperator.Division;
                result = SimpleMath.Div(lastNumber, newNumber);
                resLabel.Content = $"{result}{" "}{currentButton.Content}";
                resultHistory.Items.Insert(0, $"{lastNumber}{" "}{additionButton.Content}{" "}{newNumber}{" = "}{result}");
                resultLabel.Content = result;
            }


----
newNumber = double.Parse(resultLabel.Content.ToString());
                            result = SimpleMath.Add(temp, newNumber); //중간 계산 값

                            resLabel.Content = $"{result}{" "}{currentButton.Content}";
                            resultLabel.Content = result;
                            resultHistory.Items.Insert(0, $"{temp}{" "}{additionButton.Content}{" "}{newNumber}{" = "}{result}");
                            break;

