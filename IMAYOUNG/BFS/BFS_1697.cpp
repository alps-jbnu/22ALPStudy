#include <bits/stdc++.h>
using namespace std;

//2538_영역 구하기 
//입력받은 좌표를 통해 넓이를 차지하게 한다. 
//bfs를 돌려서 차지한 넓이를 제외한.. 연속된 부분을 찾는다. 

int board[101][101];

int d1[4] = { 0,0,1,-1};
int d2[4] = { 1,-1,0,0 };

int cnt = 0; //도형의 개수

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int m, n, k;

	cin >> m >> n >> k;

	for (int i = 0; i <= m; i++) {
		for (int j = 0; j <= n; j++) {
			cin >> board[];
		}
	}

	for (int i = 1; i <= k; i++) {
		cin >> d1[] >> d2[];
	}

	queue <pair<int, int>> q;
	//bfs 구현.. 
	while (!q.empty()) {
		
			if (x < 0 || x >= n ) continue;
			if () continue;

	}
	
	//영역의 넓이 구하기 
	
	

	//오름차순 구현
	//오름차순은 배열로 구현하나?


	cout << cnt << "\n";
	cout << 

	
}

